<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.azimgaurav.smartstudentassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NotesDetailsActivity"
            android:label="@string/title_activity_notes_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CourseDetailsActivity"
            android:label="@string/title_activity_course_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AttendanceDetails"
            android:label="@string/title_activity_attendance_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ExpenseAnalysis" />
        <activity android:name=".FoodExpense"></activity>
    </application>

</manifest>